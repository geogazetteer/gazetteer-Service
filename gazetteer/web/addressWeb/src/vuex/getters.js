const getters = {
  isOpenCoordinate: state =>state.newdev.isOpenCoordinate,
  canSelPoint: state =>state.newdev.canSelPoint,
  searchPointCoordArr: state =>state.newdev.searchPointCoordArr,
};
export default getters
